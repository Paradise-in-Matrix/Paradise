
** State

A short file to store the web workers state. Everything else is in reframe

This may be move-able there too, but will let it remain for now.


#+begin_src clojurescript :tangle ../../src/client/state.cljs
(ns client.state)

(defonce !engine-pool (atom nil))
#+end_src
